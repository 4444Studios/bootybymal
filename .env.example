# Optional CORS proxy for Google Form submissions.
# If unset, the site posts directly via a hidden iframe (works on GitHub Pages).
# Deploy worker/ with Wrangler and paste the URL here (and as a GitHub Actions secret).
VITE_FORM_PROXY_URL=
